{
  "name": "ai-job",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/app.js && node --check src/ime-guard.js && node --check src/core/company-intake.js && node --check scripts/process-company-intake.mjs && node --check scripts/refresh-jobs-scoped.mjs && node scripts/check.mjs",
    "check": "npm run lint && npm test",
    "refresh:jobs": "node scripts/refresh-jobs-scoped.mjs",
    "build:source-registry": "node scripts/build-source-registry.mjs",
    "build:company-requests": "node scripts/build-company-requests.mjs",
    "serve": "python3 -m http.server 4173"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
